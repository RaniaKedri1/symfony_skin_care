{% extends 'base.html.twig' %}

{% block title %}New categorie{% endblock %}

{% block body %}
    <div class="container mb-5">
        <h1 class="mt-5">Créer nouvelle categorie</h1>
 
 
 {{ form_start(form, {'attr': {'class': 'form'}}) }}

                    <div class="form-group">
                        {{ form_label(form.nom) }}
                        {{ form_widget(form.nom, {'attr': {'class': 'form-control', 'placeholder': 'Category Name'}}) }}
                    </div>

                    <div class="form-group">
                        {{ form_label(form.description) }}
                        {{ form_widget(form.description, {'attr': {'class': 'form-control', 'placeholder': 'Category Description'}}) }}
                    </div>
                    
                    <div class="form-group mt-3">
                        <button type="submit" class="btn btn-primary">Save</button>
                    </div>

                {{ form_end(form) }}
    <a href="{{ path('listProduit') }}" class="btn btn-secondary mt-3">Back to list</a>
    </div>
{% endblock %}
